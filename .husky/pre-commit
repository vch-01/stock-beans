#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"
npx --no-install lint-staged

# Run a fast typecheck before allowing the commit (fail on errors)
echo "Running TypeScript type check..."
npm run typecheck --silent
